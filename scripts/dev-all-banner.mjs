console.log(`
trail-memory 开发服务（pnpm dev:all）

  backend  → http://localhost:3000        （API，本机）
  frontend → http://localhost:5173        （用户端；局域网见 Vite 输出的 Network 地址）
  admin    → http://localhost:5174        （后台；局域网见 Vite 输出的 Network 地址）

`)
