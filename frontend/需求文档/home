# 需求文档（PRD）：印记管理页（瀑布流首页）

## 一、 页面概述
本页面是移动端应用的核心主页，旨在让创作者管理其创建的“实体印记”（照片与轨迹）。页面采用极简、留白的视觉风格，突出高质量的户外风景照片。

## 二、 核心数据模型说明
在当前页面中，每个“印记”需要包含以下核心业务字段：
- 印记 ID：系统生成的唯一标识。
- 印记标题：用户填写的名称。
- 封面图：用于瀑布流展示的图片。
- 公开状态：开启或关闭（开启则游客可看，关闭则仅对自己展示）。
- 自定义链接后缀：用户可自定义的附加字符串，用于拼接分享链接。

## 三、 界面布局说明（自上而下）

### 1. 顶部操作区 (Header)
- **展示规则**：页面滚动时吸顶。如果当前用户的印记列表为空，则完全隐藏此区域。
- **搜索框**：占据左侧绝大部分宽度，带有搜索图标，提示语为“搜索印记”。
- **新增按钮**：右侧的一个极简的 `+` 图标，点击热区需足够大。

### 2. 内容展示区 (Waterfall Grid)
- **正常状态**：
  - 采用**双列瀑布流**的形式向下无缝延伸。
  - **单张卡片结构**：
    - 上半部：封面图（要求高度自适应图片原比例，带轻微圆角）。
    - 下半部：左侧为印记标题（超过一行需打点省略），最右侧为一个浅灰色的 `...`（更多操作）图标。
- **空状态 (Empty State)**：
  - 当列表没有任何数据时展示。
  - 视觉呈现：屏幕正中央放置一个表示“空盒子”或“3D模型”的占位图标。
  - 提示文案：“行囊空空，去走一段路，把山野装进这里”。
  - 操作指引：文案下方提供一个醒目的主色调按钮“+ 封存新印记”。

### 3. 悬浮底部导航栏 (Floating Tabbar)
- **视觉要求**：悬浮在屏幕底部正中央的“药丸状”模块，带有一点弥散阴影以脱离背景层。
- **导航项**：
  - 仅包含左右两个纯图标，**不要任何文字**。
  - 左侧为“主页/网格”图标：当前页面高亮展示（纯黑色，且图标下方带一个微小的黑色指示点）。
  - 右侧为“用户/人像”图标：非当前页，呈现浅灰色未选中状态。

## 四、 交互与业务逻辑定义

### 1. 基础页面交互
- **搜索逻辑**：用户在输入框键入文字时，列表需实时根据“印记标题”进行模糊匹配过滤。
- **新建印记**：点击顶部的 `+` 按钮，或空状态下的主按钮，跳转至【新建印记页】。
- **进入预览**：点击瀑布流卡片的主体区域（图片或标题），跳转至【游客展示页（C面）】以预览当前印记的对外展示效果。
- **底部切换**：点击底部导航栏右侧的人像图标，跳转至【个人中心页】。

### 2. 卡片菜单交互 (核心操作链路)
当用户点击某张卡片右下角的 `...` 图标时：
- **注意阻断**：该点击**不能**触发上述的“进入预览”动作。
- **唤起菜单**：从屏幕底部滑出一个操作面板（Action Sheet）。
- **菜单项及点击响应**：
  1. **复制感应链接**：将该印记的完整链接写入手机系统剪贴板（链接规则：`https://[域名]/m/[印记ID]-[自定义链接后缀]`）。操作成功后页面顶部弹出轻提示“复制成功”。
  2. **展出设置**：关闭当前菜单面板，接着从底部弹起【展出设置弹窗】（详见下文）。
  3. **编辑印记**：跳转至【编辑印记页】并带入当前数据。
  4. **删除**：选项文字需标红。点击后在屏幕中央弹出二次确认框，用户点击“确认”后，将该数据从列表中永久移除。

### 3. 【展出设置】弹窗业务逻辑
该弹窗用于管理该印记的隐私和链接后缀兜底配置。
- **视觉形式**：从屏幕底部弹出的半屏面板，带有“展出设置”标题。
- **配置项 1：公开展示**：
  - 一个左右拨动的开关。读取并控制当前印记的“公开状态”数据。
- **配置项 2：修改感应后缀**：
  - 包含一个输入框。
  - **输入限制**：前缀部分（如 `/m/印记ID-`）锁定且不可编辑。用户只能输入并修改后缀部分（限制仅可输入字母和数字）。
  - **安全警告**：在此输入框下方，必须始终展示一个底色醒目的警告模块，文案：“⚠️ 修改后缀后，原感应链接将失效，必须重新使用 NFC 工具写入实体模型”。
- **保存操作**：
  - 弹窗最底部提供一个“保存并复制新链接”的大按钮。
  - 点击后：保存当前配置，关闭弹窗，并自动按照新规则生成链接写入剪贴板，弹出轻提示“设置已保存，新链接已复制”。

## 五、 交付要求
请作为前端开发，根据以上业务文档，自行决定技术实现细节，输出完整的、高还原度且交互顺畅的单页面级代码。包含所需的本地 Mock 数据以供直接预览测试。